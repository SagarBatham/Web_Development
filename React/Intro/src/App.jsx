import { Fragment } from "react";

export const x=2;

const App = () => {
  return (
    <>
      <div>Hello World Is New World<br />
      x={x}</div>
    <div>
      <h1>My Name is Sagar</h1>
    </div>
    </>
  )
}

export default App

//A function component will always return HTML

//We cannot write anything after return

//We can only return single data/variable/entity/value

//There is must be a single return in a function and that must